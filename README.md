# Rune
An extremely simple and lightweight code editor.

## Python port

Rune also ships as a single-file Python/Tkinter editor:

```powershell
python .\rune.py .
```

It uses only the Python standard library and runs on Windows and Linux with Tkinter available.

